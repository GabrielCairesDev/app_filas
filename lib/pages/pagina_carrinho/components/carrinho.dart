import 'package:flutter/material.dart';

class ListaCarrinho extends StatelessWidget {
  const ListaCarrinho({super.key, required int index});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
